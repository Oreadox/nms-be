spring:
    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        url: jdbc:mysql://localhost:3306/db?serverTimezone=GMT%2B8
        username: user
        password: user

server:
    port: 8081

logging:
    level:
        com.news.nms: debug

